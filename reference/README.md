# Reference Apps

These folders are sanitized reference copies only.

They are not the active implementation target.

- `app1_author_feedback`: existing FastAPI/React author profile and feedback system.
- `app2_cluster_intelligence`: existing Streamlit cluster intelligence prototype.

The unified MVP should be implemented in the root `backend/` and `frontend/` folders.

